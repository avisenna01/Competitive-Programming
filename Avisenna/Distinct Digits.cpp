#include <bits/stdc++.h>

using namespace std;

int numlen(int num)
{
}

int check(int inp)
{
    int temp;
}

int main()
{

    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int l, r;
    cin >> l >> r;

    for (int i = l; i <= r; i++)
    {
        check(i);
    }
    return 0;
}